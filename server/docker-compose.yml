version: '3.3'  
services:  
  app:
    build:
      context: ./app
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
    ports:
      - "5000:5000"
  redis:
    image: redis:5
    volumes:
      - redis_data:/data
volumes:  
  redis_data:
